# Claude tmp files
*tmpclaude*

# Node modules
node_modules/

# OS/machine specific
*.log
.DS_Store

# Nested Git repos — ignore contents but keep the folder via .gitkeep
/projects/*
!/projects/.gitkeep

# Python
__pycache__/
*.pyc
.venv/

# Environment secrets
.env

# Langfuse persistent data (bind mounts)
infra/data/

# Runtime config and secrets (created by save-config / save-secrets)
config.json
secrets.json

# Generated at runtime (install-agent-config.sh)
.vscode/
.devcontainer/firewall-domains.conf

# Upstream plugins (downloaded at install time from anthropics/claude-code)
agent-config/plugins/plugin-dev/
agent-config/plugins/frontend-design/

# Sandbox-only (private fork content — not part of the public NMC repo)
agent-config/plugins/nmcs/
agent-config/skills/projects/SKILL.md
.planning/
review/
TODO.md
CHANGELOG.md

# Host-side Claude Code artifacts (not part of container config)
.claude/

# pnpm
./pnpm-store/